<% @offers.each do |offer| %>
  <%= offer.title %>
  <%= offer.body %>
  <%= offer.price %>
  <%= offer.photo %>
  <%= offer.coupon %>

<% end %>

<%= link_to "Criar nova oferta", new_admin_offer_path   %>